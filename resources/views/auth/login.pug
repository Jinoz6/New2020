<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        meta(name="csrf-token", content=(custom_csrf || '') )
        title Login

        link(rel="shortcut icon", href="/images/logo.jpg", type="image/x-icon")

        link(rel='stylesheet', href='/stylesheets/login.css')

    body

        form.login-container.container(method="POST")

            input(type="hidden", name="_csrf" value=(custom_csrf || '') )
    
            br
            
            .columns(style="padding-left: 20px; padding-right: 20px;")
                .column.col-3.hide-xs

                .column.col-sm-12.col-6

                    .columns(style="text-align: center; justify-content: center;")
                        img(src="/images/logo.jpg", alt="Logo", style="width: 150px;")

                    br

                    if params.has_error
                        .columns
                            .column(style="padding: 0;")
                                .toast.toast-error(style='padding:12px;')
                                    span Username ຫຼື ລະຫັດຜ່ານ ບໍ່ຖືກຕ້ອງ.

                        br

                    .columns
                        .field(style="width: 100%;")
                            .has-icon-left
                                input.form-input#username(type="text", name="username", placeholder="Username")
                                i.form-icon.fas.fa-user

                    br

                    .columns
                        .field(style="width: 100%;")
                            .has-icon-left
                                input.form-input#password(type="password", name="password", placeholder="Password")
                                i.form-icon.fas.fa-lock

                .column.col-3.hide-xs

            br

            .columns(style="padding-left: 20px; padding-right: 20px;")
                .column.col-3.hide-xs
                .column.col-sm-12.col-6
                    button.btn.btn-success(style="width: 100%;") LOGIN
                .column.col-3.hide-xs
                